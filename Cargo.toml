[package]
name = "relaxed-memory-concurrency"
version = "0.2.0"
edition = "2024"

[features]
check-loom = ["loom"]

[dependencies]
cfg-if = "1.0.4"
lazy_static = "1.5.0"
loom = { version = "0.7.2", optional = true }
